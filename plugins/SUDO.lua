do

function run(msg, matches)
  return [[
  sudo user :
  @negative_officiall
  
  ]]

  end
return {
  description = "shows sudoers", 
  usage = "!sudoers : return sudousers",
  patterns = {
    "^SUDOERS$",
    "^/sudoers$",
    "^!sudoers$",
    "^sudoers$",
    "^ادمین$"
  },
  run = run
}
end
